export const paths = {
  login: "/login",
  register: "/register",
  documents: "/documents",
  document: ":id",
  home: "/",
};

/* Move to vite .env file */
export const serverURL = "http://localhost:3001";
